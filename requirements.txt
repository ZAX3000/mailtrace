# --- Core web ---
Flask==3.0.3              # your HTTP API framework
Werkzeug==3.0.3           # Flask's WSGI/HTTP utilities (Flask depends on it; pin for reproducibility)
Jinja2==3.1.4             # required by Flask even if you don't render templates
itsdangerous==2.2.0       # signing utilities used by Flask (sessions, etc.)

# --- Database ---
SQLAlchemy==2.0.31
Flask-SQLAlchemy==3.1.1
alembic==1.13.2
Flask-Migrate==4.1.0
psycopg[binary]==3.2.3    # Postgres driver

# --- Config/env ---
python-dotenv==1.0.1
Authlib==1.3.1

# --- HTTP / integrations ---
requests==2.32.3
stripe==9.8.0

# --- Matching / fuzzy compare ---
rapidfuzz==3.9.7

# --- Compatibility (helps avoid resolver issues in CI and with some libs) ---
typing-extensions>=4.9.0

